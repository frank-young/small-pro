<view class="page__bd task">
    <view class="shadow-box">
      <view class="head-title">第{{id}}天</view>
      <view class="item-text">1.这是测试的内容</view>
      <view class="item-text">2.这是测试的内容,这是测试的内容这是测试的内容这是测试的内容这是测试的内容这是测试的内容这是测试的内容这是测试的内容</view>
      <view class="success-box" hidden="{{isComplete}}">
        <icon type="success" size="52"></icon>
        <view class="success-text">任务完成</view>
      </view>
    </view>
    <view class="weui-cells upload-box">
        <view class="weui-cell">
            <view class="weui-cell__bd">
                <view class="weui-uploader">
                    <view class="weui-uploader__hd">
                        <view class="weui-uploader__title">
                            图片上传
                            <text class="text-tips">
                                点击下面「 + 」选择并上传图片
                            </text>
                        </view>
                        <view class="weui-uploader__info">{{files.length}}/9</view>
                    </view>
                    <view class="weui-uploader__bd">
                        <view class="weui-uploader__files" id="uploaderFiles">
                            <block wx:for-items="{{files}}" wx:key="{{index}}">
                                <view class="weui-uploader__file"  catchlongtap="showDeleteBox" id="{{item}}">
                                    <view class="icon-box" hidden="{{isDelete}}" catchtap="deleteImage">
                                       <icon type="clear" size="22" color="#F43530"></icon>
                                    </view>
                                    <image class="weui-uploader__img" catchtap="previewImage" src="{{item}}" mode="aspectFill" />
                                </view>
                            </block>
                        </view>
                        <view class="weui-uploader__input-box" hidden="{{disabledUpload}}">
                            <view class="weui-uploader__input" bindtap="chooseImage"></view>
                        </view>
                    </view>
                </view>
            </view>
        </view>
        <view class="ctrl-box">
            文字
            <view class="ctrl-left" bindtap="previousTask" hover-class="weui-cell_active">
              <text class="arrow-left"></text>
            </view>
            <view class="ctrl-right" bindtap="nextTask" hover-class="weui-cell_active">
              <text class="arrow-right"></text>
            </view>
        </view>
    </view>
</view>
